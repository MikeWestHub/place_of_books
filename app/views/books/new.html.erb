<h1>Add Books</h1>
<p>Add to the selection below</p>

<%= form_for @book do |f| %>
  <fieldset>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </fieldset>

  <fieldset>
    <%= f.label :author_id %>
    <%= f.text_field :author_id %>
  </fieldset>

  <%= f.submit %>

<% end %>
